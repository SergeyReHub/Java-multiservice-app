grpc:
  server:
    port: 9090

logging:
  level:
    com.gateway: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/LogsDatabase
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
app:
  rd-conf:
    hostname: localhost
    port: 6379
